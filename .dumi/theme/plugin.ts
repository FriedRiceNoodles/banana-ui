import { IApi } from 'dumi';
import {join} from "path"

export default (api: IApi) => {
  // api.addTmpGenerateWatcherPaths(() => join(__dirname,'../../node_modules/@banana'));
  // console.log('>>>>>>>>>>>>>>>>', join(__dirname,'../../forceRender.md'));
  // api.addTmpGenerateWatcherPaths(() => '../../forceRender.md')
};
